<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itsm.misantecosunidos">

    <application
        android:name=".data.model.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MisantecosUnidos">
        <activity android:name=".ui.view.Products"></activity>
        <activity android:name=".ui.view.RegisterBusiness" />
        <activity android:name=".ui.view.RegisterUser" />
        <activity android:name=".ui.view.Register" />
        <activity android:name=".ui.view.MenuBusiness" />
        <activity
            android:name=".ui.view.Login"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.view.MenuUser" />
    </application>

</manifest>